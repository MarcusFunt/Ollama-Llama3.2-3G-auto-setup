# Ollama-Llama3.2-3G-auto-setup
An automatic install and set up script that installs llama 3.2 and make sure that all the dependencies are set up. It runs in docker so that it's easy to pull from github
